---
const props = Astro.props
---

<blockquote {...props}>
   <slot class="slot" />
</blockquote>
<style lang="scss">
   @use '../styles/_main-colors' as *;

   blockquote {
     color: $text;
     font-family: 'Space Mono', sans-serif;
     text-indent: 0;
     padding-left: 1rem;
     margin-left: 2px;
     border-left: solid;
     border-color: $accent;
     padding-top: 3px;
     padding-bottom: 3px;
   }
   
   blockquote :global(p) {
      margin-bottom: 0;
   }

</style>
